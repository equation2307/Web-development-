{
  // what we call our thing
  "name": "recipe-app-backend",
  "version": "1.0.0",
  "description": "Backend server for the recipe application",
  "main": "server.js",
  // stuff we can run
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  // stuff we need to make it work
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  // stuff we need just for development
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  // what version of node we need
  "engines": {
    "node": ">=18.16.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
